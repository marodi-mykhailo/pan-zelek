# 🔧 Виправлення проблеми з продуктами на GitHub Pages

## ✅ Що вже виправлено:

1. ✅ API URL правильно налаштований: `https://web-production-101aa.up.railway.app/api`
2. ✅ CORS налаштування оновлені в коді
3. ✅ Build згенерований правильно

## 🔄 Що потрібно зробити:

### Крок 1: Перезапустити бекенд на Railway

1. Зайдіть в Railway: https://railway.app
2. Відкрийте ваш проект
3. Відкрийте ваш сервіс (backend)
4. Натисніть на **"Deployments"** або **"Settings"**
5. Знайдіть кнопку **"Redeploy"** або **"Restart"**
6. Або просто зробіть commit і push змін (Railway автоматично перезапустить)

**Або через Git:**
```bash
git add backend/src/server.ts
git commit -m "Fix CORS for GitHub Pages"
git push
```

Railway автоматично перезапустить сервіс після push.

### Крок 2: Перевірте що бекенд працює

Відкрийте в браузері:
```
https://web-production-101aa.up.railway.app/api/products
```

**Очікуваний результат:**
```json
{
  "products": [
    {
      "id": "...",
      "namePl": "...",
      ...
    }
  ]
}
```

**Якщо повертає `[]` (порожній масив):**
- Потрібно виконати seed бази даних в Railway Shell

### Крок 3: Якщо база порожня - виконайте seed

1. Зайдіть в Railway → ваш сервіс → **Shell**
2. Виконайте:
   ```bash
   npm run db:push
   npm run db:seed
   ```

### Крок 4: Перевірте GitHub Pages

1. Зачекайте 2-3 хвилини після перезапуску бекенду
2. Відкрийте: https://marodi-mykhailo.github.io/pan-zelek/
3. Натисніть **Ctrl+Shift+R** (Windows/Linux) або **Cmd+Shift+R** (Mac) для жорсткого оновлення
4. Відкрийте **Developer Tools** (F12) → **Console**
5. Перевірте чи є помилки

### Крок 5: Перевірте Network запити

1. Відкрийте **Developer Tools** (F12) → **Network**
2. Перезавантажте сторінку
3. Знайдіть запит до `products`
4. Перевірте:
   - **URL:** має бути `https://web-production-101aa.up.railway.app/api/products`
   - **Status:** має бути `200` (OK)
   - **Response:** має містити масив продуктів

---

## 🐛 Якщо все ще не працює:

### Перевірте CORS помилки

Якщо в консолі є помилка:
```
Access to fetch at 'https://web-production-101aa.up.railway.app/api/products'
from origin 'https://marodi-mykhailo.github.io' has been blocked by CORS policy
```

**Рішення:**
1. Зайдіть в Railway → ваш сервіс → **Variables**
2. Додайте або оновіть:
   - **Variable:** `FRONTEND_URL`
   - **Value:** `https://marodi-mykhailo.github.io`
3. Railway автоматично перезапустить сервіс

### Перевірте що змінні оточення правильні

В Railway → ваш сервіс → **Variables** мають бути:
- `DATABASE_URL` - URL до PostgreSQL бази
- `JWT_SECRET` - секретний ключ для JWT
- `FRONTEND_URL` (опціонально) - `https://marodi-mykhailo.github.io`

---

## ✅ Очікуваний результат:

Після виконання всіх кроків:
- ✅ Бекенд працює і повертає продукти
- ✅ GitHub Pages завантажує продукти без помилок
- ✅ Немає CORS помилок в консолі
- ✅ Продукти відображаються на сторінці `/pan-zelek/products`

---

## 📝 Надішліть для допомоги:

Якщо все ще не працює, надішліть:

1. **Скріншот консолі браузера** (F12 → Console)
2. **Скріншот Network вкладки** (F12 → Network → запит до products)
3. **Що повертає:** https://web-production-101aa.up.railway.app/api/products
4. **Скріншот Railway Variables** (щоб перевірити змінні оточення)
