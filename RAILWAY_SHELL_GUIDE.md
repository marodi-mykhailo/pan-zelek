# 🐚 Як знайти Shell в Railway

## 📍 Де знайти Shell:

### Варіант 1: Через головну панель сервісу

1. Зайдіть на https://railway.app
2. Відкрийте ваш проект
3. Відкрийте ваш **Web Service** (бекенд)
4. Вгорі є вкладки: **Deployments**, **Metrics**, **Logs**, **Settings**
5. Натисніть на **"Deployments"**
6. Праворуч вгорі або внизу знайдіть кнопку **"Shell"** або **"Open Shell"** (іконка терміналу 📟)

### Варіант 2: Через останній деплой

1. Відкрийте **Deployments**
2. Натисніть на останній deployment (останній рядок)
3. В деталях деплою знайдіть кнопку **"Shell"** або **"Open Shell"**

### Варіант 3: Через Settings

1. Відкрийте **Settings** сервісу
2. Знайдіть розділ **"Shell"** або **"Terminal"**

---

## 🚀 Що робити в Shell:

Після відкриття Shell ви побачите термінал. Виконайте команди:

```bash
# 1. Створити таблиці в базі даних
npm run db:push

# 2. Додати тестові дані (продукти та адміна)
npm run db:seed
```

**Після виконання цих команд:**
- ✅ Створяться всі таблиці в базі даних
- ✅ Додадуться тестові продукти
- ✅ Створиться адмін користувач:
  - Email: `admin@panzelek.pl`
  - Password: `admin123`

---

## 💡 Альтернатива: Railway CLI

Якщо не можете знайти Shell в інтерфейсі, використайте Railway CLI локально:

```bash
# 1. Встановіть CLI
npm i -g @railway/cli

# 2. Увійдіть
railway login

# 3. Підключіться до проекту
railway link

# 4. Запустіть команди в контейнері Railway
railway run npm run db:push
railway run npm run db:seed
```

---

## 🆘 Якщо Shell не відкривається:

1. Переконайтеся що сервіс активний (не спить)
2. Спробуйте оновити сторінку
3. Перевірте що ви маєте доступ до проекту
4. Використайте Railway CLI як альтернативу

---

## 📸 Візуальна інструкція:

Shell зазвичай знаходиться:
- **Вкладка "Deployments"** → кнопка "Shell" праворуч вгорі
- Або в деталях deployment → кнопка "Open Shell"
- Іноді може бути в **Settings** → розділ "Shell"

Шукайте іконку терміналу або текст "Shell" / "Open Shell" / "Terminal"
