# 🚀 Гайд по деплою бекенду - Pan Żelek

## 📋 Варіанти хостингу

### Рекомендовані варіанти:

1. **Railway** ⭐⭐⭐ (Найпростіший - РЕКОМЕНДОВАНО)
   - ✅ Безкоштовний план ($5 кредитів/місяць)
   - ✅ Автоматичний деплой з GitHub
   - ✅ Вбудована підтримка PostgreSQL
   - ✅ Просте налаштування
   - ✅ Швидкий деплой

2. **Render** ⭐⭐ (Безкоштовний)
   - ✅ Безкоштовний план (з обмеженнями)
   - ✅ Автоматичний деплой з GitHub
   - ✅ PostgreSQL доступний
   - ⚠️ Може "засинати" після неактивності (прокидається за ~30 сек)

3. **Fly.io** ⭐⭐
   - ✅ Безкоштовний план
   - ✅ Швидкий деплой
   - ✅ Глобальна мережа
   - ⚠️ Потрібен CLI для налаштування

4. **DigitalOcean App Platform**
   - ✅ Простий у використанні
   - ⚠️ Платний (від $5/місяць)

---

## 🚂 Варіант 1: Railway (Рекомендовано)

### Крок 1: Підготовка проекту

✅ **Файли вже готові!** В проекті вже є:
- `Procfile` - для Railway/Heroku
- `railway.json` - конфігурація для Railway
- `render.yaml` - конфігурація для Render

1. Переконайтеся що всі зміни закомічені:
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push
   ```

### Крок 2: Налаштування Railway

1. Зайдіть на https://railway.app
2. Увійдіть через GitHub
3. Натисніть "New Project"
4. Оберіть "Deploy from GitHub repo"
5. Оберіть ваш репозиторій `pan-zelek`

### Крок 3: Налаштування бази даних

1. В Railway проекті натисніть "+ New"
2. Оберіть "Database" → "PostgreSQL"
3. Railway автоматично створить базу та додасть змінну `DATABASE_URL`

### Крок 4: Налаштування змінних середовища

В Railway проекті:
1. Відкрийте ваш сервіс (Web Service)
2. Перейдіть в "Variables"
3. Додайте змінні:
   ```
   NODE_ENV=production
   PORT=3000
   JWT_SECRET=ваш_секретний_ключ_мінімум_32_символи
   DATABASE_URL=автоматично_додається_з_бази_даних
   ```

### Крок 5: Налаштування деплою

✅ **Конфігурація вже готова!** Файл `railway.json` вже створений.

Railway автоматично визначить налаштування з `railway.json`. Якщо потрібно змінити вручну:

1. В налаштуваннях сервісу (Settings → Deploy):
   - **Root Directory:** залиште порожнім
   - Railway використає `railway.json` автоматично

### Крок 6: Запуск міграцій та seed

1. В Railway відкрийте "Deployments"
2. Натисніть на останній деплой
3. Відкрийте "View Logs"
4. В "Settings" → "Connect" → "Generate Domain" отримайте URL

5. Запустіть міграції через Railway CLI або через вкладку "Shell":
   ```bash
   # Встановіть Railway CLI
   npm i -g @railway/cli

   # Увійдіть
   railway login

   # Підключіться до проекту
   railway link

   # Запустіть міграції
   railway run npm run db:push
   railway run npm run db:seed
   ```

### Крок 7: Оновлення фронтенду

Після отримання URL бекенду (наприклад: `https://your-app.up.railway.app`):

**Варіант A: Через змінні середовища (рекомендовано)**

1. Створіть файл `.env.production` в корені проекту:
   ```
   VITE_API_URL=https://your-app.up.railway.app
   ```

2. При збірці Vite автоматично використає цю змінну:
   ```bash
   npm run build
   ```

**Варіант B: Для GitHub Pages**

1. Оновіть `vite.config.ts`:
   ```typescript
   export default defineConfig({
     plugins: [react()],
     base: '/pan-zelek/',
     define: {
       'import.meta.env.VITE_API_URL': JSON.stringify('https://your-app.up.railway.app')
     }
   });
   ```

2. Або використайте GitHub Secrets для GitHub Actions (якщо використовуєте Actions для деплою)

**Варіант C: Для Vercel/Netlify**

1. В налаштуваннях проекту додайте змінну:
   - Name: `VITE_API_URL`
   - Value: `https://your-app.up.railway.app`

---

## 🎨 Варіант 2: Render

### Крок 1: Підготовка

1. Створіть `render.yaml` в корені:
   ```yaml
   services:
     - type: web
       name: pan-zelek-backend
       env: node
       buildCommand: npm install && cd backend && npm run build
       startCommand: cd backend && npm start
       envVars:
         - key: NODE_ENV
           value: production
         - key: JWT_SECRET
           generateValue: true
         - key: DATABASE_URL
           fromDatabase:
             name: pan-zelek-db
             property: connectionString

   databases:
     - name: pan-zelek-db
       plan: free
       databaseName: panzelek
       user: panzelek
   ```

### Крок 2: Деплой

1. Зайдіть на https://render.com
2. Увійдіть через GitHub
3. Натисніть "New +" → "Blueprint"
4. Оберіть ваш репозиторій
5. Render автоматично визначить налаштування з `render.yaml`

### Крок 3: Налаштування

1. Після створення сервісу:
   - Додайте змінні середовища вручну якщо потрібно
   - Запустіть міграції через "Shell" або через CLI

---

## 🪂 Варіант 3: Fly.io

### Крок 1: Встановлення CLI

```bash
curl -L https://fly.io/install.sh | sh
fly auth login
```

### Крок 2: Створення додатку

```bash
cd backend
fly launch
```

### Крок 3: Налаштування

1. Створіть `backend/fly.toml`:
   ```toml
   app = "pan-zelek-backend"
   primary_region = "waw"

   [build]

   [env]
     NODE_ENV = "production"
     PORT = "3000"

   [[services]]
     internal_port = 3000
     protocol = "tcp"

     [[services.ports]]
       handlers = ["http"]
       port = 80
       force_https = true

     [[services.ports]]
       handlers = ["tls", "http"]
       port = 443
   ```

2. Додайте базу даних:
   ```bash
   fly postgres create --name pan-zelek-db
   fly postgres attach pan-zelek-db
   ```

3. Додайте секрети:
   ```bash
   fly secrets set JWT_SECRET=ваш_секретний_ключ
   ```

4. Деплой:
   ```bash
   fly deploy
   ```

---

## 📝 Загальні кроки для всіх платформ

### 1. Підготовка package.json

Переконайтеся що в `backend/package.json` є:
```json
{
  "scripts": {
    "start": "node dist/server.js",
    "build": "tsc",
    "dev": "tsx watch src/server.ts"
  }
}
```

### 2. Створення .dockerignore (опціонально)

```
node_modules
dist
.env
*.log
.git
```

### 3. Налаштування CORS

Переконайтеся що в `backend/src/server.ts` CORS налаштований правильно:
```typescript
app.use(cors({
  origin: process.env.FRONTEND_URL || 'http://localhost:5173',
  credentials: true
}));
```

### 4. Запуск міграцій

Після деплою:
```bash
# Через CLI платформи або через вкладку Shell
npm run db:push
npm run db:seed
```

---

## 🔧 Налаштування фронтенду для production

### Варіант A: GitHub Pages (статичний фронтенд)

1. Оновіть `vite.config.ts`:
   ```typescript
   export default defineConfig({
     plugins: [react()],
     base: '/pan-zelek/',
     // ...
   });
   ```

2. Оновіть API URL в `.env.production`:
   ```
   VITE_API_URL=https://your-backend-url.com
   ```

3. Деплой:
   ```bash
   npm run build
   npm run deploy
   ```

### Варіант B: Vercel/Netlify (рекомендовано для фронтенду)

1. Підключіть репозиторій до Vercel/Netlify
2. Налаштуйте змінні середовища:
   - `VITE_API_URL=https://your-backend-url.com`
3. Build команда: `npm run build`
4. Output directory: `dist`

---

## ✅ Чеклист після деплою

- [ ] Бекенд запущений та доступний
- [ ] База даних підключена
- [ ] Міграції виконані (`db:push`)
- [ ] Seed дані додані (`db:seed`)
- [ ] Змінні середовища налаштовані
- [ ] CORS налаштований для фронтенду
- [ ] Фронтенд оновлений з новим API URL
- [ ] Тестування: логування працює
- [ ] Тестування: API запити працюють

---

## 🆘 Troubleshooting

### Проблема: Бекенд не запускається
- Перевірте логи в панелі платформи
- Переконайтеся що `PORT` змінна налаштована
- Перевірте що `npm start` команда правильна

### Проблема: База даних не підключається
- Перевірте `DATABASE_URL` змінну
- Переконайтеся що база створена та активна
- Перевірте що Prisma може підключитися

### Проблема: CORS помилки
- Додайте URL фронтенду в CORS налаштування
- Перевірте що `credentials: true` встановлено

---

## 📚 Корисні посилання

- Railway: https://railway.app/docs
- Render: https://render.com/docs
- Fly.io: https://fly.io/docs
- Prisma Deployment: https://www.prisma.io/docs/guides/deployment

---

## 🎯 Швидкий старт з Railway (5 хвилин)

1. **Зареєструйтесь:** https://railway.app (через GitHub)

2. **Створіть проект:**
   - "New Project" → "Deploy from GitHub repo"
   - Оберіть `pan-zelek`

3. **Додайте базу даних:**
   - "+ New" → "Database" → "PostgreSQL"
   - Railway автоматично додасть `DATABASE_URL`

4. **Додайте змінні середовища:**
   - В налаштуваннях сервісу → "Variables"
   - Додайте:
     ```
     NODE_ENV=production
     JWT_SECRET=ваш_довгий_випадковий_ключ_мінімум_32_символи
     PORT=3000
     ```

5. **Запустіть міграції:**
   - Відкрийте "Shell" в Railway
   - Виконайте:
     ```bash
     npm run db:push
     npm run db:seed
     ```

6. **Отримайте URL:**
   - Settings → "Generate Domain"
   - Скопіюйте URL (наприклад: `https://pan-zelek-production.up.railway.app`)

7. **Оновіть фронтенд:**
   - Створіть `.env.production`:
     ```
     VITE_API_URL=https://pan-zelek-production.up.railway.app
     ```
   - Перебудьте фронтенд: `npm run build`

Готово! 🎉
