# 🐛 Debug GitHub Pages - Продукти не завантажуються

## ✅ Що перевірити:

### 1. Перевірте що GitHub Pages оновився

GitHub Pages може оновлюватися до 5-10 хвилин. Перевірте:
- Відкрийте: https://marodi-mykhailo.github.io/pan-zelek/
- Натисніть Ctrl+Shift+R (Windows/Linux) або Cmd+Shift+R (Mac) для жорсткого оновлення
- Перевірте консоль браузера (F12) - які помилки?

### 2. Перевірте API запити в Network

1. Відкрийте Developer Tools (F12)
2. Перейдіть на вкладку **Network**
3. Перезавантажте сторінку
4. Знайдіть запит до `/api/products` або `products`
5. Подивіться:
   - **URL запиту** - має бути `https://web-production-101aa.up.railway.app/api/products`
   - **Status** - має бути 200 (OK) або 404/500 (помилка)
   - **Response** - що повертає сервер?

### 3. Перевірте CORS помилки

Якщо в консолі є помилка типу:
```
Access to fetch at 'https://web-production-101aa.up.railway.app/api/products'
from origin 'https://marodi-mykhailo.github.io' has been blocked by CORS policy
```

**Рішення:** Потрібно оновити CORS на бекенді в Railway.

### 4. Перевірте що бекенд працює

Відкрийте в браузері:
- https://web-production-101aa.up.railway.app/api/products

Має повернути JSON з продуктами. Якщо ні - проблема на бекенді.

### 5. Перевірте що міграції виконані

Якщо бекенд повертає порожній масив `[]` - можливо не виконані міграції в Railway.

---

## 🔧 Швидке рішення:

### Крок 1: Перевірте бекенд напряму

Відкрийте в браузері:
```
https://web-production-101aa.up.railway.app/api/products
```

**Очікуваний результат:**
```json
{
  "products": [
    {
      "id": "...",
      "namePl": "...",
      ...
    }
  ]
}
```

### Крок 2: Якщо бекенд не працює

1. Зайдіть в Railway
2. Відкрийте Shell
3. Виконайте:
   ```bash
   npm run db:push
   npm run db:seed
   ```

### Крок 3: Якщо CORS помилка

1. Зайдіть в Railway → ваш сервіс → Variables
2. Додайте або оновіть:
   - **Variable:** `FRONTEND_URL`
   - **Value:** `https://marodi-mykhailo.github.io`
3. Перезапустіть сервіс (Railway зробить це автоматично після зміни змінних)

### Крок 4: Перевірте build

Переконайтеся що в build правильний API URL:

```bash
npm run build
# Перевірте dist/index.html - шляхи мають починатися з /pan-zelek/
```

---

## 📝 Що надіслати для допомоги:

Якщо все ще не працює, надішліть:

1. **Скріншот консолі браузера (F12 → Console)**
2. **Скріншот Network вкладки (F12 → Network → знайдіть запит до products)**
3. **Що повертає:** https://web-production-101aa.up.railway.app/api/products

---

## ✅ Очікуваний результат:

Після виправлення:
- ✅ Сторінка завантажується без білого екрану
- ✅ Продукти відображаються на сторінці `/pan-zelek/products`
- ✅ Немає помилок в консолі
- ✅ Network запити до `/api/products` повертають статус 200
