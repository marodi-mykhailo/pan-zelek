# 🚀 Швидкий старт - Pan Żelek

## ⚡ Швидкий старт (3 кроки):

### 1. Запустіть проект:
```bash
npm run dev:all
```

### 2. Відкрийте браузер:
- Frontend: http://localhost:5173
- Backend API: http://localhost:3000/api/products

### 3. Увійдіть як адмін:
- Email: `admin@panzelek.pl`
- Password: `admin123`
- Адмінка: http://localhost:5173/admin

---

## 📋 Детальна інструкція:

### Крок 1: Перевірка налаштувань

Переконайтеся, що файл `backend/.env` існує та містить правильний `DATABASE_URL`.

### Крок 2: Заповніть базу даних

```bash
npm run db:seed
```

Це створить:
- ✅ Адміна: `admin@panzelek.pl` / `admin123`
- ✅ Тестові товари

### Крок 3: Запуск проекту

### Варіант А: Запуск frontend + backend разом (рекомендовано)

```bash
npm run dev:all
```

Це запустить:
- Frontend на http://localhost:5173
- Backend API на http://localhost:3000

### Варіант Б: Запуск окремо

**Тільки frontend:**
```bash
npm run dev
```
→ Відкриється на http://localhost:5173

**Тільки backend:**
```bash
npm run dev:server
```
→ API буде на http://localhost:3000

## Крок 3: Відкрийте браузер

1. Відкрийте http://localhost:5173
2. Перевірте чи працює API: http://localhost:3000/api/products

## Що тестувати:

1. ✅ Каталог товарів - http://localhost:5173/products
2. ✅ Додавання товарів до кошика
3. ✅ Кошик - http://localhost:5173/cart
4. ✅ Конструктор боксів - http://localhost:5173/box-builder
5. ✅ Реєстрація/вхід - http://localhost:5173/login
6. ✅ Оформлення замовлення - http://localhost:5173/checkout
7. ✅ Профіль - http://localhost:5173/profile (після входу)

## Якщо щось не працює:

1. Перевірте чи працює база даних:
   ```bash
   npm run db:push
   ```

2. Перевірте чи є дані в базі:
   ```bash
   npm run db:seed
   ```

3. Перевірте логи в консолі на помилки

4. Переконайтеся що порти 5173 та 3000 вільні

## Зупинка проекту:

Натисніть `Ctrl + C` в терміналі де запущений проект.
