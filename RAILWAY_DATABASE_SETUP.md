# 🔧 Налаштування DATABASE_URL в Railway

## 🐛 Проблема:
```
error: Environment variable not found: DATABASE_URL.
```

## ✅ Рішення:

### Варіант 1: Якщо база даних вже створена в Railway

1. Зайдіть в Railway: https://railway.app
2. Відкрийте ваш проект
3. Знайдіть ваш **PostgreSQL сервіс** (база даних)
4. Відкрийте вкладку **Variables**
5. Знайдіть змінну `DATABASE_URL` або `POSTGRES_URL` або `PGDATABASE`
6. Скопіюйте значення

### Варіант 2: Якщо база даних НЕ створена

1. Зайдіть в Railway → ваш проект
2. Натисніть **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматично створить базу даних
4. Відкрийте створений PostgreSQL сервіс
5. Перейдіть на вкладку **Variables**
6. Знайдіть змінну `DATABASE_URL` або `POSTGRES_URL`
7. Скопіюйте значення

### Варіант 3: Якщо використовуєте зовнішню базу (Neon, Supabase, тощо)

1. Зайдіть в Railway → ваш проект → ваш backend сервіс
2. Перейдіть на вкладку **Variables**
3. Натисніть **"+ New Variable"**
4. Введіть:
   - **Name:** `DATABASE_URL`
   - **Value:** ваш connection string (наприклад: `postgresql://user:password@host:port/database?sslmode=require`)
5. Натисніть **"Add"**

---

## 🔗 Додавання DATABASE_URL до Backend сервісу

### Крок 1: Знайдіть DATABASE_URL

1. Зайдіть в Railway → ваш проект
2. Відкрийте ваш **PostgreSQL сервіс** (база даних)
3. Перейдіть на вкладку **Variables**
4. Знайдіть змінну `DATABASE_URL` або `POSTGRES_URL` або `PGDATABASE`
5. Скопіюйте значення (має виглядати як: `postgresql://user:password@host:port/database`)

### Крок 2: Додайте до Backend сервісу

1. Зайдіть в Railway → ваш проект
2. Відкрийте ваш **Backend сервіс** (не базу даних!)
3. Перейдіть на вкладку **Variables**
4. Натисніть **"+ New Variable"**
5. Введіть:
   - **Name:** `DATABASE_URL`
   - **Value:** вставте скопійоване значення з кроку 1
6. Натисніть **"Add"**

### Крок 3: Railway автоматично перезапустить сервіс

Після додавання змінної Railway автоматично перезапустить ваш backend сервіс.

---

## 🔍 Якщо використовуєте Neon або іншу зовнішню базу:

### Для Neon:
1. Зайдіть в Neon Dashboard
2. Відкрийте ваш проект
3. Перейдіть на вкладку **Connection Details**
4. Скопіюйте **Connection String** (має починатися з `postgresql://`)
5. Додайте в Railway Variables як `DATABASE_URL`

### Для Supabase:
1. Зайдіть в Supabase Dashboard
2. Відкрийте ваш проект
3. Перейдіть на **Settings** → **Database**
4. Скопіюйте **Connection String** з розділу "Connection string"
5. Додайте в Railway Variables як `DATABASE_URL`

---

## ✅ Перевірка після налаштування:

1. Зайдіть в Railway → ваш backend сервіс → **Deployments**
2. Перевірте що сервіс перезапустився
3. Перевірте логи - не повинно бути помилки `Environment variable not found: DATABASE_URL`
4. Відкрийте в браузері:
   ```
   https://web-production-101aa.up.railway.app/api/products
   ```
5. Має повернути JSON з продуктами (може бути порожній масив `[]` якщо не виконано seed)

---

## 🔧 Після додавання DATABASE_URL:

### Виконайте міграції в Railway Shell:

1. Зайдіть в Railway → ваш backend сервіс → **Shell**
2. Виконайте послідовно:
   ```bash
   # Генеруємо Prisma Client
   npm run db:generate:prod

   # Створюємо таблиці в базі
   npm run db:push

   # Заповнюємо базу тестовими даними
   npm run db:seed
   ```

---

## 📝 Формат DATABASE_URL:

Правильний формат:
```
postgresql://username:password@host:port/database?sslmode=require
```

Приклад:
```
postgresql://neondb_owner:password@ep-icy-mode-ag6b7lop-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

---

## ⚠️ Важливо:

- `DATABASE_URL` має бути додана до **Backend сервісу**, а не до бази даних
- Після додавання змінної Railway автоматично перезапустить сервіс
- Переконайтеся що URL правильний і база даних доступна
