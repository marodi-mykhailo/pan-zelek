# 🔧 Виправлення помилки 500 на бекенді

## 🐛 Проблема:
```
GET https://web-production-101aa.up.railway.app/api/products 500 (Internal Server Error)
```

Це означає що:
- ✅ CORS працює (запит доходить до сервера)
- ✅ API URL правильний
- ❌ Проблема на бекенді - сервер падає з помилкою

## 🔍 Можливі причини:

### 1. Prisma Client не згенерований
**Симптоми:** Помилка типу `@prisma/client did not initialize yet`

**Рішення:**
1. Зайдіть в Railway → ваш сервіс → **Shell**
2. Виконайте:
   ```bash
   npm run db:generate:prod
   ```

### 2. База даних не підключена
**Симптоми:** Помилка підключення до бази даних

**Рішення:**
1. Зайдіть в Railway → ваш сервіс → **Variables**
2. Перевірте що є змінна `DATABASE_URL`
3. Переконайтеся що URL правильний (має починатися з `postgresql://`)

### 3. Таблиці не створені в базі
**Симптоми:** Помилка типу `relation "Product" does not exist`

**Рішення:**
1. Зайдіть в Railway → ваш сервіс → **Shell**
2. Виконайте:
   ```bash
   npm run db:push
   ```

### 4. Помилка в коді бекенду
**Симптоми:** Помилка в логах Railway

**Рішення:**
1. Зайдіть в Railway → ваш сервіс → **Deployments**
2. Відкрийте останній deployment
3. Перевірте логи (вкладка **Logs**)

---

## 🔧 Покрокове виправлення:

### Крок 1: Перевірте логи Railway

1. Зайдіть в Railway: https://railway.app
2. Відкрийте ваш проект
3. Відкрийте ваш сервіс (backend)
4. Перейдіть на вкладку **Deployments**
5. Відкрийте останній deployment
6. Перевірте логи - яка саме помилка?

### Крок 2: Перевірте змінні оточення

1. Зайдіть в Railway → ваш сервіс → **Variables**
2. Перевірте що є:
   - `DATABASE_URL` - має бути URL до PostgreSQL
   - `JWT_SECRET` - має бути встановлений
   - `NODE_ENV` - має бути `production` (опціонально)

### Крок 3: Виконайте міграції в Railway Shell

1. Зайдіть в Railway → ваш сервіс → **Shell**
2. Виконайте послідовно:
   ```bash
   # Генеруємо Prisma Client
   npm run db:generate:prod

   # Створюємо таблиці в базі
   npm run db:push

   # Заповнюємо базу тестовими даними
   npm run db:seed
   ```

### Крок 4: Перезапустіть сервіс

1. Зайдіть в Railway → ваш сервіс → **Settings**
2. Знайдіть кнопку **"Redeploy"** або **"Restart"**
3. Або просто зробіть commit змін (Railway автоматично перезапустить)

---

## 📝 Найчастіші помилки:

### Помилка 1: `@prisma/client did not initialize yet`
**Рішення:**
```bash
npm run db:generate:prod
```

### Помилка 2: `relation "Product" does not exist`
**Рішення:**
```bash
npm run db:push
```

### Помилка 3: `Can't reach database server`
**Рішення:**
- Перевірте `DATABASE_URL` в Railway Variables
- Переконайтеся що база даних активна

### Помилка 4: `Environment variable not found: DATABASE_URL`
**Рішення:**
- Додайте `DATABASE_URL` в Railway Variables
- Перезапустіть сервіс

---

## ✅ Перевірка після виправлення:

1. Відкрийте в браузері:
   ```
   https://web-production-101aa.up.railway.app/api/products
   ```

2. **Очікуваний результат:**
   ```json
   {
     "products": [
       {
         "id": "...",
         "namePl": "...",
         ...
       }
     ]
   }
   ```

3. Якщо все працює - перевірте GitHub Pages:
   ```
   https://marodi-mykhailo.github.io/pan-zelek/products
   ```

---

## 🆘 Якщо все ще не працює:

Надішліть:
1. **Скріншот логів Railway** (Deployments → останній deployment → Logs)
2. **Список змінних оточення** (Variables) - без значень, тільки назви
3. **Що повертає:** https://web-production-101aa.up.railway.app/api/products
