# 🔐 Доступ до адмінки - Pan Żelek

## ⚠️ ВАЖЛИВО: Система ролей активована!

**Адмінка тепер доступна ТІЛЬКИ користувачам з роллю ADMIN!**

## Як отримати доступ до адмінки:

### 1. Використайте готовий обліковий запис адміна

**Після запуску `npm run db:seed` створюється адмінський акаунт:**

- **Email:** `admin@panzelek.pl`
- **Password:** `admin123`

### 2. Увійдіть в систему

1. Запустіть проект:
   ```bash
   npm run dev:all
   ```

2. Відкрийте http://localhost:5173/login

3. Увійдіть з адмінськими даними:
   - Email: `admin@panzelek.pl`
   - Password: `admin123`

### 3. Відкрийте адмінку

Після входу:
- Перейдіть на http://localhost:5173/admin
- Або натисніть кнопку "Admin" в меню (з'явиться тільки для адмінів)

### 4. Звичайні користувачі НЕ мають доступу

Якщо звичайний користувач спробує зайти в адмінку:
- Він отримає повідомлення про помилку
- Буде перенаправлений на головну сторінку
- Кнопка "Admin" не з'явиться в меню

### 3. Створення тестових даних

#### Товари:
Товари вже є в базі даних (seed дані). Якщо їх немає, запустіть:
```bash
npm run db:seed
```

#### Замовлення:
Щоб створити тестові замовлення:
1. Додайте товари до кошика на http://localhost:5173/products
2. Оформіть замовлення на http://localhost:5173/checkout
3. Заповніть форму та завершіть замовлення

#### Користувачі:
Користувачі створюються автоматично при реєстрації.

## ✅ Система ролей реалізована!

**Адмінка тепер захищена системою ролей:**
- ✅ Поле `role` додано в модель `User` (USER/ADMIN)
- ✅ Middleware `requireAdmin` перевіряє роль
- ✅ Frontend перевіряє роль перед показом адмінки
- ✅ Адмін створюється автоматично при `npm run db:seed`

## 🔧 Створення нового адміна (вручну):

Якщо потрібно створити ще одного адміна:

```typescript
// В backend/prisma/seed.ts або через Prisma Studio:
await prisma.user.create({
  data: {
    email: 'newadmin@panzelek.pl',
    password: await bcrypt.hash('password123', 10),
    name: 'New Admin',
    role: 'ADMIN',
  },
});
```

Або через Prisma Studio:
```bash
npm run db:studio
```
Потім знайдіть користувача та змініть `role` на `ADMIN`.

## 📊 Що можна робити в адмінці:

1. **Statystyki** - перегляд статистики магазину
2. **Zamówienia** - управління замовленнями, зміна статусів
3. **Produkty** - CRUD операції з товарами
4. **Użytkownicy** - перегляд користувачів

## 🧪 Тестові дані:

Після `npm run db:seed` у вас будуть:
- ~20 товарів різних категорій
- Всі товари доступні для замовлення

Щоб створити замовлення:
1. Зайдіть як користувач
2. Додайте товари до кошика
3. Оформіть замовлення
4. Перевірте в адмінці!
